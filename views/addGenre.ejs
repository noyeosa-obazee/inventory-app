<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Genre</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="form-card">
      <form action="new" method="POST">
        <div class="form-group full-width">
          <label for="name">Name of Genre</label>
          <input
            type="text"
            id="name"
            name="name"
            placeholder="e.g. Dark romance"
            required
          />
        </div>
        <input type="hidden" name="title" value="<%= query.title %>" />
        <input type="hidden" name="price" value="<%= query.price %>" />
        <input
          type="hidden"
          name="stock_quantity"
          value="<%= query.stock_quantity %>"
        />
        <input type="hidden" name="genre_id" value="<%= query.author_id %>" />
        <div class="form-actions">
          <button type="submit" class="btn-submit">Add Genre</button>
        </div>
      </form>
    </div>
  </body>
</html>
