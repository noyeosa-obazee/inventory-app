<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Bookstore Inventory</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="main-container">
      <header>
        <h1>📚 Inventory Dashboard</h1>
        <a href="/books/new" class="btn-add">+ Add New Book</a>
      </header>

      <div class="book-grid">
        <% if (books.length === 0) { %>
        <p class="empty-state">No books found in inventory.</p>
        <% } else { %> <% books.forEach(book => { %>
        <div class="book-card">
          <div class="card-header">
            <h2><%= book.title %></h2>
            <span class="genre-tag"><%= book.genre_name %></span>
          </div>

          <div class="card-body">
            <p class="author">By <strong><%= book.author_name %></strong></p>

            <div class="details-row">
              <span class="price">$<%= book.price %></span>

              <span
                class="stock <%= book.stock_quantity < 5 ? 'stock-low' : 'stock-good' %>"
              >
                Stock: <%= book.stock_quantity %>
              </span>
            </div>
          </div>

          <div class="card-actions">
            <a href="/books/update/<%= book.id %>" class="btn-edit">Edit</a>
            <a href="/books/delete/<%= book.id %>" class="btn-delete">Delete</a>
          </div>
        </div>
        <% }) %> <% } %>
      </div>
    </div>
  </body>
</html>
